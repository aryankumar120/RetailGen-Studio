{
  "name": "retailgen-studio",
  "version": "1.0.0",
  "description": "AI-Adaptive Creative Builder for Retail Media",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\" \"npm run dev:ai\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "dev:ai": "cd ai-service && python3 app.py",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install && cd ../ai-service && pip3 install -r requirements.txt"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
